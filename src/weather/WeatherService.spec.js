import WeatherService from "./WeatherService.js"

describe('WeatherService', () => {
    it('does stuff', () => {

        // let req = new XMLHttpRequest;
        // req.responseText = 'rraa';
        // console.log(req);

        // let outcome = new WeatherService('raaa');
        // console.log(outcome.forecast());
    });
});